{
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^22.13.8"
  },
  "scripts": {
    "test:json": "npx playwright test --reporter=json > playwright-report.json"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "dotenv": "^16.5.0",
    "playwright-slack-reporter": "^1.0.0"
  }
}
